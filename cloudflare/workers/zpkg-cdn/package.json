{
  "name": "@zed-pkg/zpkg-cdn",
  "private": true,
  "type": "module",
  "description": "Public read-only Worker in front of the zed-pkg artifact bucket",
  "scripts": {
    "test": "node --test 'test/*.test.mjs'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
