# Supabase consolidation

This provider root was consolidated from `zed-pkg/zed-supabase` on 2026-09-14.

Canonical ownership is now `zed-pkg/zed-infra` under `supabase/zxjvldcyqobiupeiwjhx/`. The standalone provider repository should become read-only after the migration PR merges. Provider project identity remains explicit so Supabase GitHub integration can use this directory as its project working root without mixing Terraform state.
